#!/usr/bin/env python3

#!/usr/bin/python3				(funciona pero con env usamos el path para encontrar python3 si esta instalado en otra ruta que no sea la porDefecto)

if __name__ == 'main':

print("Ready")
