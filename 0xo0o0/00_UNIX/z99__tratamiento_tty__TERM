
				::: ssh user_test@(mvnb) bash			"ejecuta el comando antes de ingresar en una restrictedBash"

::: script /dev/null -c bash

::: Ctrl-z

::: stty raw -echo ;fg

::: reset xterm								"se va a poner negro men, no output, pero sigue funcionando, dale Ctrl-l o Ctrl-c, va a refrescar pantalla <3"

			::: echo $SHELL && export SHELL=/bin/bash			"opcional"

::: echo $TERM && export TERM=xterm						"obligatorio"

::: echo $SHELL && export SHELL=bash					"obligatorio"


*** AJUSTAR EL FILAS (ROWS) Y COLUMNAS (COLUMNS)		VIM - EMCAS - NANO

::: stty size					"en una shell del sistema principal"
		64 237

::: stty rows 64 columns 237				"shell ip targed"
