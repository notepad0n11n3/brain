#include <stdio.h>

    fprintf()
    printf()

    scanf()
    fgets()



#include <math.h>

    ceil()     Redondea un n[umero x hacia arriba
    floot()    Redondea un numero x hacia abajo

    fabs()     Devuelve el valor absoluto de un n[umero

    pow()      Calcula x elevado a la potencia y

    sqrt()     Devuelve la ra[iz cuadrada de x

    cos()      Devuelve el coseno de un angulo x

    sin()      Devuelve ele seno de un angulo x

    tan()     Devuelve la tangente de un angulo x

    exp()     Calcula la funci[on exponencial en x

    log()     Calcula el logaritmo natural en x


#include <stdlib.h>


    rand()   Genera numero aleatorio entre 0 y RAND_MAX.
    srand()  Inicializa el generador de n[umeros aleatorios

    abs()   Saca el valor absoluto de un n[umero entero

    labs()    Saca el valor absoluto de un n[umero entero largo
