#include <stdio.h>

int main(void){
  
  /*
   *
   *  %d    El dato se convierte a entero decimal.
   *  %o    El dato se convierte a octal
   *  %x    El dato se convierte a hexadecimal
   *  %u    El dato se convierte a entero sin signo
   *  %c    El dato se convierte a tipo caracter
   *  %e    El dato se convierte a tipo float.  ( notaci[on cient[ifica )
   *  %f    El dato se convierte a tipo float
   *  %g    El dato se convierte a tipo float. (se convierte a %e o a %f )
   *  %s    El dato a mostrar es una cadena de caracteres
   *  %lf   El dato se considera tipo double
   *
   *  %p    Direcci[on de memoria de una variable/puntero
   *
   */

  return 0;
}
